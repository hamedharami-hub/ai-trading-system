# تأیید digest evidence محلی

`DEC-241` فقط SHA-256 مورد انتظار را با digest بازتولیدشده از یک observation bundle تغییرناپذیر مقایسه می‌کند. خروجی `MATCH`، `MISMATCH` یا `INVALID_EXPECTED_DIGEST` است و هیچ داده‌ای را ذخیره یا ارسال نمی‌کند.
