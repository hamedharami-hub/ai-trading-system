# Digest evidence برای EURUSD M1 Replay

`DEC-239` یک observation bundle موجود را با JCS canonicalize و با SHA-256 digest می‌کند. خروجی فقط digest و metadata غیرقابل‌اجرا است؛ ذخیره‌سازی، انتقال شبکه، تصمیم معاملاتی یا هر اجرای دیگری ندارد. نبود SHA-256 به خطای fail-closed منجر می‌شود.
