# هش مجموعه‌برچسب Golden برای EURUSD / M1 / REPLAY

پس از پذیرش identity و cursor-binding مجموعه‌برچسب، این helper فقط گزارش پذیرفته‌شده را با JCS و SHA-256 به یک هش قابل‌تکرار محلی تبدیل می‌کند. مجموعه‌برچسب ردشده هش نمی‌شود.

این هش فقط evidence است: داده یا برچسب را ذخیره، منتقل، تفسیر یا تغییر نمی‌دهد و هیچ StrategyCandidate، grade، entry، Paper/Demo artifact، OrderIntent، درخواست خارجی یا قابلیت اجرا نمی‌سازد.
