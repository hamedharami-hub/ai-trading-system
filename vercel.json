{
  "buildCommand": "pnpm --filter @trade/contracts build && pnpm --filter @trade/pwa build",
  "outputDirectory": "apps/pwa/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
